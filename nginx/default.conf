server {
    listen 80 default_server;

    root /usr/share/nginx/html;
    index index.html index.htm;

    server_name rootssh.me www.rootssh.me;

    location / {
        try_files $uri $uri/ =404;
    }

    location /api/ {
        proxy_pass http://node-app:3000/;
    }
}
